# Core dependencies for Print Shop AI Order Guardrail PoC

# Web frameworks
flask>=3.0.0
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Image/PDF processing
Pillow>=10.0.0
pillow-heif>=0.13.0
pdf2image>=1.16.0
PyPDF2>=3.0.0
PyMuPDF>=1.23.0

# Data handling
python-dateutil>=2.8.0

# Optional: For LLM integration (add your preferred LLM library)
# openai>=1.0.0
# langchain>=0.1.0
# anthropic>=0.7.0

# Note: pdf2image requires poppler-utils. Install with:
# macOS: brew install poppler
# Ubuntu/Debian: sudo apt-get install poppler-utils
# Windows: Download poppler from https://github.com/oschwartz10612/poppler-windows/releases
